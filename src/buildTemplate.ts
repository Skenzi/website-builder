
const firstTemplate = () => {
    
}

const buildTemplate = (activeTemplate: any) => {
    switch(activeTemplate) {
        case 'firstTemlate': {
            
        }
    }
}

export default buildTemplate;
